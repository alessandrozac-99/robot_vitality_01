<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.vitality">

    <!-- ===================================================== -->
    <!-- PERMESSI -->
    <!-- ===================================================== -->
    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />

    <!-- Foreground service permissions -->
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE" />
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE_DATA_SYNC" />

    <!-- Required on Android 13+ for foreground notifications -->
    <uses-permission android:name="android.permission.POST_NOTIFICATIONS" />

    <!-- Exact alarms (Android 12+) -->
    <uses-permission android:name="android.permission.SCHEDULE_EXACT_ALARM" />

    <!-- Avvio dopo reboot -->
    <uses-permission android:name="android.permission.RECEIVE_BOOT_COMPLETED" />

    <uses-permission android:name="com.robotemi.sdk.permission.MAP" />


    <!-- ===================================================== -->
    <!-- APPLICATION -->
    <!-- ===================================================== -->
    <application
        android:label="Vitality"
        android:icon="@mipmap/ic_launcher"
        android:theme="@style/Theme.Vitality">

        <!-- ===================================================== -->
        <!-- MAIN ACTIVITY -->
        <!-- ===================================================== -->
        <activity
            android:name=".MainActivity"
            android:exported="true"
            android:screenOrientation="landscape">
            <intent-filter>
                <action android:name="android.intent.action.MAIN"/>
                <category android:name="android.intent.category.LAUNCHER"/>
            </intent-filter>
        </activity>


        <!-- ===================================================== -->
        <!-- SERVIZI -->
        <!-- ===================================================== -->

        <!-- Aggregation Service (già presente) -->
        <service
            android:name=".service.VitalityAggregationService"
            android:exported="false"
            android:foregroundServiceType="dataSync" />

        <!-- Comfort Coaching Service (NUOVO) -->
        <service
            android:name=".service.ComfortCoachService"
            android:exported="false"
            android:foregroundServiceType="dataSync" />


        <!-- ===================================================== -->
        <!-- RECEIVERS -->
        <!-- ===================================================== -->

        <receiver
            android:name=".receivers.DailyStartReceiver"
            android:exported="false">
            <intent-filter>
                <action android:name="com.example.vitality.ACTION_DAILY_START" />
            </intent-filter>
        </receiver>

        <receiver
            android:name=".receivers.BootCompletedReceiver"
            android:exported="false">
            <intent-filter>
                <action android:name="android.intent.action.BOOT_COMPLETED" />
                <action android:name="android.intent.action.LOCKED_BOOT_COMPLETED" />
            </intent-filter>
        </receiver>

        <receiver
            android:name=".receivers.StartAggregationReceiver"
            android:exported="true" />

        <receiver
            android:name=".receivers.StopAggregationReceiver"
            android:exported="true" />

        <!-- Skill Temi -->
        <meta-data
            android:name="com.robotemi.sdk.metadata.SKILL"
            android:value="@string/app_name" />

        <!-- Permessi Temi -->
        <meta-data
            android:name="@string/metadata_permissions"
            android:value="com.robotemi.permission.map,com.robotemi.permission.settings"/>

    </application>

</manifest>
